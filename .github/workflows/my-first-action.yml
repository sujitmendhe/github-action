name: Learning Github action

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Simulate Build
        run: echo "Compiling code...Build Successfully"

      - name: Run Tests
        run: |
          echo "Running unit tests..."
          exit 1
      - name: Report Failure
        if: failure()
        run: echo "Alert! The test failed"

