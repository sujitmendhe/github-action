name: Learning Github action

on: [push]

jobs:
  say-hello:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Greet the User
        run: echo "Hello! this is my first automated pipeline"

      - name: how the Date
        run: date
